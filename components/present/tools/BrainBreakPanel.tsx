/**
 * Placeholder architecture only (Phase 4 scope) - a minimal shell so the
 * tray's six tools are all present; a future phase can implement real
 * brain-break content without touching the tray itself.
 */
export function BrainBreakPanel() {
  return <p className="text-xs text-falcon-cream-200/60">Brain Break activities are coming in a future update.</p>;
}
